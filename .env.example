# Optional. Vesper does not require cloud AI at runtime.
# XAI_API_KEY=
# VESPER_ENV=production
# VESPER_SKIP_DISCOVERY=0
# VESPER_LLAMA_BACKEND=vulkan
# Optimizer live mode is off unless you point this at a real specialist API.
# VESPER_OPTIMIZER_ENDPOINT=
