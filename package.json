{
  "name": "vesper",
  "private": true,
  "type": "module",
  "description": "Local-first personal AI assistant for Windows. Not Mortis. Not the PC optimizer.",
  "scripts": {
    "test": "node --experimental-strip-types --test src/vesper/**/*.test.ts"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "zod": "^4.4.0"
  }
}
